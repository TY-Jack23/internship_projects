# -*- coding: utf-8 -*-
"""AI 智能层（人员4负责，需求 3.X.1 / 3.X.2 / 3.X.4）。

本子包承载除 LLM 本地化部署以外的人员4 全部功能：
  * 意图识别（intent/）：自然语言 -> 标准化查询意图 + 参数
  * 文本生成（summary/）：结构化分析结果 -> 自然语言摘要
  * 服务编排（service.py）：对上层 REST API 暴露统一调用入口

设计原则：
  1. 不直接依赖 Spark / 大数据分析服务内部实现，仅通过 service 层抽象调用；
  2. LLM 客户端可插拔（OpenAI / DeepSeek / Mock / Disabled），API key 留空自动降级；
  3. 意图识别一期采用规则引擎（带训练集验证准确率 >= 90%），二期可平滑升级为 ML 模型；
  4. 文本生成带幻觉检查（关键数字一致性）与隐私脱敏（不外泄原始记录）。
"""
